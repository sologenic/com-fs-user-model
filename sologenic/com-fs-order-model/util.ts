// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v1.181.2
<<<<<<< HEAD
//   protoc               v6.32.0
=======
//   protoc               v6.32.1
>>>>>>> 4f72533d1504faafa54c95ac833197f5afe9bf79
// source: sologenic/com-fs-order-model/util.proto

/* eslint-disable */
import _m0 from "protobufjs/minimal";
import { Timestamp } from "../../google/protobuf/timestamp";

export const protobufPackage = "order";

export enum OrderType {
  NOT_USED_ORDER_TYPE = 0,
  /** ORDER_TYPE_PURCHASE - Avoid naming conflicts with PURCHASE in the action enum */
  ORDER_TYPE_PURCHASE = 1,
  ORDER_TYPE_SELL = 2,
  UNRECOGNIZED = -1,
}

export function orderTypeFromJSON(object: any): OrderType {
  switch (object) {
    case 0:
    case "NOT_USED_ORDER_TYPE":
      return OrderType.NOT_USED_ORDER_TYPE;
    case 1:
    case "ORDER_TYPE_PURCHASE":
      return OrderType.ORDER_TYPE_PURCHASE;
    case 2:
    case "ORDER_TYPE_SELL":
      return OrderType.ORDER_TYPE_SELL;
    case -1:
    case "UNRECOGNIZED":
    default:
      return OrderType.UNRECOGNIZED;
  }
}

export function orderTypeToJSON(object: OrderType): string {
  switch (object) {
    case OrderType.NOT_USED_ORDER_TYPE:
      return "NOT_USED_ORDER_TYPE";
    case OrderType.ORDER_TYPE_PURCHASE:
      return "ORDER_TYPE_PURCHASE";
    case OrderType.ORDER_TYPE_SELL:
      return "ORDER_TYPE_SELL";
    case OrderType.UNRECOGNIZED:
    default:
      return "UNRECOGNIZED";
  }
}

export enum TimeInForce {
  NOT_USED_TIME_IN_FORCE = 0,
  DAY = 1,
  /** GOOD_TIL_CANCELED - GTC */
  GOOD_TIL_CANCELED = 2,
  /** AT_THE_OPENING - OPG */
  AT_THE_OPENING = 3,
  /** AT_THE_CLOSE - CLS */
  AT_THE_CLOSE = 4,
  /** IMMEDIATE_OR_CANCEL - IOC */
  IMMEDIATE_OR_CANCEL = 5,
  /** FILL_OR_KILL - FOK */
  FILL_OR_KILL = 6,
  /** GOOD_TIL_TIME - GTT */
  GOOD_TIL_TIME = 7,
  UNRECOGNIZED = -1,
}

export function timeInForceFromJSON(object: any): TimeInForce {
  switch (object) {
    case 0:
    case "NOT_USED_TIME_IN_FORCE":
      return TimeInForce.NOT_USED_TIME_IN_FORCE;
    case 1:
    case "DAY":
      return TimeInForce.DAY;
    case 2:
    case "GOOD_TIL_CANCELED":
      return TimeInForce.GOOD_TIL_CANCELED;
    case 3:
    case "AT_THE_OPENING":
      return TimeInForce.AT_THE_OPENING;
    case 4:
    case "AT_THE_CLOSE":
      return TimeInForce.AT_THE_CLOSE;
    case 5:
    case "IMMEDIATE_OR_CANCEL":
      return TimeInForce.IMMEDIATE_OR_CANCEL;
    case 6:
    case "FILL_OR_KILL":
      return TimeInForce.FILL_OR_KILL;
    case 7:
    case "GOOD_TIL_TIME":
      return TimeInForce.GOOD_TIL_TIME;
    case -1:
    case "UNRECOGNIZED":
    default:
      return TimeInForce.UNRECOGNIZED;
  }
}

export function timeInForceToJSON(object: TimeInForce): string {
  switch (object) {
    case TimeInForce.NOT_USED_TIME_IN_FORCE:
      return "NOT_USED_TIME_IN_FORCE";
    case TimeInForce.DAY:
      return "DAY";
    case TimeInForce.GOOD_TIL_CANCELED:
      return "GOOD_TIL_CANCELED";
    case TimeInForce.AT_THE_OPENING:
      return "AT_THE_OPENING";
    case TimeInForce.AT_THE_CLOSE:
      return "AT_THE_CLOSE";
    case TimeInForce.IMMEDIATE_OR_CANCEL:
      return "IMMEDIATE_OR_CANCEL";
    case TimeInForce.FILL_OR_KILL:
      return "FILL_OR_KILL";
    case TimeInForce.GOOD_TIL_TIME:
      return "GOOD_TIL_TIME";
    case TimeInForce.UNRECOGNIZED:
    default:
      return "UNRECOGNIZED";
  }
}

export enum ProcessState {
  NOT_USED_PROCESS_STATE = 0,
  PROCESS_STATE_OPEN = 1,
  PROCESS_STATE_LOCKED = 2,
  PROCESS_STATE_FAILED = 3,
  PROCESS_STATE_PROCESSED = 4,
  UNRECOGNIZED = -1,
}

export function processStateFromJSON(object: any): ProcessState {
  switch (object) {
    case 0:
    case "NOT_USED_PROCESS_STATE":
      return ProcessState.NOT_USED_PROCESS_STATE;
    case 1:
    case "PROCESS_STATE_OPEN":
      return ProcessState.PROCESS_STATE_OPEN;
    case 2:
    case "PROCESS_STATE_LOCKED":
      return ProcessState.PROCESS_STATE_LOCKED;
    case 3:
    case "PROCESS_STATE_FAILED":
      return ProcessState.PROCESS_STATE_FAILED;
    case 4:
    case "PROCESS_STATE_PROCESSED":
      return ProcessState.PROCESS_STATE_PROCESSED;
    case -1:
    case "UNRECOGNIZED":
    default:
      return ProcessState.UNRECOGNIZED;
  }
}

export function processStateToJSON(object: ProcessState): string {
  switch (object) {
    case ProcessState.NOT_USED_PROCESS_STATE:
      return "NOT_USED_PROCESS_STATE";
    case ProcessState.PROCESS_STATE_OPEN:
      return "PROCESS_STATE_OPEN";
    case ProcessState.PROCESS_STATE_LOCKED:
      return "PROCESS_STATE_LOCKED";
    case ProcessState.PROCESS_STATE_FAILED:
      return "PROCESS_STATE_FAILED";
    case ProcessState.PROCESS_STATE_PROCESSED:
      return "PROCESS_STATE_PROCESSED";
    case ProcessState.UNRECOGNIZED:
    default:
      return "UNRECOGNIZED";
  }
}

export interface ProcessInfo {
  ProcessState: ProcessState;
  ProcessedAt: Date | undefined;
}

export interface LockLogRecord {
  Key: string;
  MustHaveState: ProcessState[];
  TargetState: ProcessState;
}

/**
 * The `Key` field represents the datastore key for the `Order` message.
 * The `KeyPrefix` field is used to create a key for broker or smart contract logs (prefix + key = key for logs).
 * The `IsNew` field identifies if the record linked to the key is new. This is helpful for recovery because
 * both the rescanner and the recovery service might pick up the same record. When creating log records, if `IsNew` is true, the Key is sent
 * through the channel for processing. Otherwise, it isn't sent, as an existing key signals the recovery service will handle it.
 */
export interface Key {
  Key: string;
  KeyPrefix?: string | undefined;
  IsNew?: boolean | undefined;
}

export interface InstanceID {
  InstanceID: string;
}

function createBaseProcessInfo(): ProcessInfo {
  return { ProcessState: 0, ProcessedAt: undefined };
}

export const ProcessInfo = {
  encode(message: ProcessInfo, writer: _m0.Writer = _m0.Writer.create()): _m0.Writer {
    if (message.ProcessState !== 0) {
      writer.uint32(8).int32(message.ProcessState);
    }
    if (message.ProcessedAt !== undefined) {
      Timestamp.encode(toTimestamp(message.ProcessedAt), writer.uint32(18).fork()).ldelim();
    }
    return writer;
  },

  decode(input: _m0.Reader | Uint8Array, length?: number): ProcessInfo {
    const reader = input instanceof _m0.Reader ? input : _m0.Reader.create(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseProcessInfo();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          if (tag !== 8) {
            break;
          }

          message.ProcessState = reader.int32() as any;
          continue;
        case 2:
          if (tag !== 18) {
            break;
          }

          message.ProcessedAt = fromTimestamp(Timestamp.decode(reader, reader.uint32()));
          continue;
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skipType(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): ProcessInfo {
    return {
      ProcessState: isSet(object.ProcessState) ? processStateFromJSON(object.ProcessState) : 0,
      ProcessedAt: isSet(object.ProcessedAt) ? fromJsonTimestamp(object.ProcessedAt) : undefined,
    };
  },

  toJSON(message: ProcessInfo): unknown {
    const obj: any = {};
    if (message.ProcessState !== 0) {
      obj.ProcessState = processStateToJSON(message.ProcessState);
    }
    if (message.ProcessedAt !== undefined) {
      obj.ProcessedAt = message.ProcessedAt.toISOString();
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<ProcessInfo>, I>>(base?: I): ProcessInfo {
    return ProcessInfo.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<ProcessInfo>, I>>(object: I): ProcessInfo {
    const message = createBaseProcessInfo();
    message.ProcessState = object.ProcessState ?? 0;
    message.ProcessedAt = object.ProcessedAt ?? undefined;
    return message;
  },
};

function createBaseLockLogRecord(): LockLogRecord {
  return { Key: "", MustHaveState: [], TargetState: 0 };
}

export const LockLogRecord = {
  encode(message: LockLogRecord, writer: _m0.Writer = _m0.Writer.create()): _m0.Writer {
    if (message.Key !== "") {
      writer.uint32(10).string(message.Key);
    }
    writer.uint32(18).fork();
    for (const v of message.MustHaveState) {
      writer.int32(v);
    }
    writer.ldelim();
    if (message.TargetState !== 0) {
      writer.uint32(24).int32(message.TargetState);
    }
    return writer;
  },

  decode(input: _m0.Reader | Uint8Array, length?: number): LockLogRecord {
    const reader = input instanceof _m0.Reader ? input : _m0.Reader.create(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseLockLogRecord();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          if (tag !== 10) {
            break;
          }

          message.Key = reader.string();
          continue;
        case 2:
          if (tag === 16) {
            message.MustHaveState.push(reader.int32() as any);

            continue;
          }

          if (tag === 18) {
            const end2 = reader.uint32() + reader.pos;
            while (reader.pos < end2) {
              message.MustHaveState.push(reader.int32() as any);
            }

            continue;
          }

          break;
        case 3:
          if (tag !== 24) {
            break;
          }

          message.TargetState = reader.int32() as any;
          continue;
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skipType(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): LockLogRecord {
    return {
      Key: isSet(object.Key) ? globalThis.String(object.Key) : "",
      MustHaveState: globalThis.Array.isArray(object?.MustHaveState)
        ? object.MustHaveState.map((e: any) => processStateFromJSON(e))
        : [],
      TargetState: isSet(object.TargetState) ? processStateFromJSON(object.TargetState) : 0,
    };
  },

  toJSON(message: LockLogRecord): unknown {
    const obj: any = {};
    if (message.Key !== "") {
      obj.Key = message.Key;
    }
    if (message.MustHaveState?.length) {
      obj.MustHaveState = message.MustHaveState.map((e) => processStateToJSON(e));
    }
    if (message.TargetState !== 0) {
      obj.TargetState = processStateToJSON(message.TargetState);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<LockLogRecord>, I>>(base?: I): LockLogRecord {
    return LockLogRecord.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<LockLogRecord>, I>>(object: I): LockLogRecord {
    const message = createBaseLockLogRecord();
    message.Key = object.Key ?? "";
    message.MustHaveState = object.MustHaveState?.map((e) => e) || [];
    message.TargetState = object.TargetState ?? 0;
    return message;
  },
};

function createBaseKey(): Key {
  return { Key: "", KeyPrefix: undefined, IsNew: undefined };
}

export const Key = {
  encode(message: Key, writer: _m0.Writer = _m0.Writer.create()): _m0.Writer {
    if (message.Key !== "") {
      writer.uint32(10).string(message.Key);
    }
    if (message.KeyPrefix !== undefined) {
      writer.uint32(18).string(message.KeyPrefix);
    }
    if (message.IsNew !== undefined) {
      writer.uint32(24).bool(message.IsNew);
    }
    return writer;
  },

  decode(input: _m0.Reader | Uint8Array, length?: number): Key {
    const reader = input instanceof _m0.Reader ? input : _m0.Reader.create(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseKey();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          if (tag !== 10) {
            break;
          }

          message.Key = reader.string();
          continue;
        case 2:
          if (tag !== 18) {
            break;
          }

          message.KeyPrefix = reader.string();
          continue;
        case 3:
          if (tag !== 24) {
            break;
          }

          message.IsNew = reader.bool();
          continue;
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skipType(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): Key {
    return {
      Key: isSet(object.Key) ? globalThis.String(object.Key) : "",
      KeyPrefix: isSet(object.KeyPrefix) ? globalThis.String(object.KeyPrefix) : undefined,
      IsNew: isSet(object.IsNew) ? globalThis.Boolean(object.IsNew) : undefined,
    };
  },

  toJSON(message: Key): unknown {
    const obj: any = {};
    if (message.Key !== "") {
      obj.Key = message.Key;
    }
    if (message.KeyPrefix !== undefined) {
      obj.KeyPrefix = message.KeyPrefix;
    }
    if (message.IsNew !== undefined) {
      obj.IsNew = message.IsNew;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<Key>, I>>(base?: I): Key {
    return Key.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<Key>, I>>(object: I): Key {
    const message = createBaseKey();
    message.Key = object.Key ?? "";
    message.KeyPrefix = object.KeyPrefix ?? undefined;
    message.IsNew = object.IsNew ?? undefined;
    return message;
  },
};

function createBaseInstanceID(): InstanceID {
  return { InstanceID: "" };
}

export const InstanceID = {
  encode(message: InstanceID, writer: _m0.Writer = _m0.Writer.create()): _m0.Writer {
    if (message.InstanceID !== "") {
      writer.uint32(10).string(message.InstanceID);
    }
    return writer;
  },

  decode(input: _m0.Reader | Uint8Array, length?: number): InstanceID {
    const reader = input instanceof _m0.Reader ? input : _m0.Reader.create(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseInstanceID();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          if (tag !== 10) {
            break;
          }

          message.InstanceID = reader.string();
          continue;
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skipType(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): InstanceID {
    return { InstanceID: isSet(object.InstanceID) ? globalThis.String(object.InstanceID) : "" };
  },

  toJSON(message: InstanceID): unknown {
    const obj: any = {};
    if (message.InstanceID !== "") {
      obj.InstanceID = message.InstanceID;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<InstanceID>, I>>(base?: I): InstanceID {
    return InstanceID.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<InstanceID>, I>>(object: I): InstanceID {
    const message = createBaseInstanceID();
    message.InstanceID = object.InstanceID ?? "";
    return message;
  },
};

type Builtin = Date | Function | Uint8Array | string | number | boolean | undefined;

export type DeepPartial<T> = T extends Builtin ? T
  : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends {} ? { [K in keyof T]?: DeepPartial<T[K]> }
  : Partial<T>;

type KeysOfUnion<T> = T extends T ? keyof T : never;
export type Exact<P, I extends P> = P extends Builtin ? P
  : P & { [K in keyof P]: Exact<P[K], I[K]> } & { [K in Exclude<keyof I, KeysOfUnion<P>>]: never };

function toTimestamp(date: Date): Timestamp {
  const seconds = Math.trunc(date.getTime() / 1_000);
  const nanos = (date.getTime() % 1_000) * 1_000_000;
  return { seconds, nanos };
}

function fromTimestamp(t: Timestamp): Date {
  let millis = (t.seconds || 0) * 1_000;
  millis += (t.nanos || 0) / 1_000_000;
  return new globalThis.Date(millis);
}

function fromJsonTimestamp(o: any): Date {
  if (o instanceof globalThis.Date) {
    return o;
  } else if (typeof o === "string") {
    return new globalThis.Date(o);
  } else {
    return fromTimestamp(Timestamp.fromJSON(o));
  }
}

function isSet(value: any): boolean {
  return value !== null && value !== undefined;
}
